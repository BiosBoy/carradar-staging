import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carradar.app',
  appName: 'carradar',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
